version: '3.8'

services:
  muro-app:
    build: .
    container_name: muro-interactivo-container
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    stdin_open: true
    tty: true
